# ============================================================================
#  Gemi Teknik Doküman Asistanı - Bağımlılıklar (requirements.txt)
# ----------------------------------------------------------------------------
#  ÖNERİLEN PYTHON SÜRÜMÜ: 3.11 veya 3.12
#  (PyTorch / EasyOCR / sentence-transformers için Python 3.14'te henüz
#   kararlı wheel'ler bulunmayabilir. Sanal ortamı 3.11/3.12 ile kurun.)
#
#  Kurulum:
#     python -m venv .venv
#     .venv\Scripts\activate
#     pip install -r requirements.txt
# ============================================================================

# --- Arayüz (UI) ---
customtkinter==5.2.2
tkinterdnd2>=0.4.0            # Dosya/klasör sürükle-bırak desteği
pillow>=10.0.0                 # Görsel önizleme ve OCR ön-işleme

# --- PDF okuma ve render (scanned PDF -> OCR için sayfa görüntüsü üretir) ---
PyMuPDF>=1.24.0               # 'fitz' modülü

# --- Metin parçalama (chunking) ---
langchain>=0.2.0
langchain-text-splitters>=0.2.0

# --- Lokal Embedding modeli ---
sentence-transformers>=2.7.0
#
# PyTorch: torch ve torchvision SÜRÜMLERİ BİRBİRİYLE EŞLEŞMELİDİR; aksi halde
# "operator torchvision::nms does not exist" hatası alınır. İkisini birlikte,
# tek komutla kurun ki pip uyumlu çifti seçsin.
#   Varsayılan (CPU): aşağıdaki satırlar PyPI'den uyumlu CPU çiftini kurar.
#   GPU (CUDA) için: ikisini eşleşen CUDA çiftiyle değiştirin (sürücüye uygun
#   cuXXX seçin), örn. CUDA 12.1:
#       pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
torch>=2.2.0
torchvision>=0.17.0            # transformers/easyocr için gerekli; torch ile eşleşmeli

# --- Lokal Vektör Veritabanı (kalıcı/persistent) ---
chromadb>=0.5.0

# --- Lokal OCR (internetsiz görsel metin okuma) ---
easyocr>=1.7.1

# --- LLM Bağlantıları ---
google-genai>=1.0.0            # Gemini API - yeni Google Gen AI SDK (internet varken)
requests>=2.31.0               # Hugging Face'ten GGUF model indirme (akışlı)
huggingface_hub>=0.20.0        # Model deposu yardımcıları (indirme/önbellek)
#
# Gömülü çevrimdışı motor: llama.cpp (Ollama GEREKMEZ). GGUF modelleri arayüzdeki
# "Modeller" sekmesinden indirilir ve bu motorla yerelde çalıştırılır.
#   Varsayılan (CPU): aşağıdaki satır PyPI'den CPU wheel'i kurar.
#   GPU (CUDA) için: CUDA destekli wheel gerekir; CUDA sürümünüze uygun indeksi
#   kullanın (torch ile aynı cuXXX). Örn. CUDA 12.1:
#       pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
#   Not: CUDA wheel'i çalışma anında cudart/cublas DLL'lerine ihtiyaç duyar; bunlar
#   torch'un cuXXX dağıtımıyla (torch/lib) gelir ve uygulama import öncesi bu klasörü
#   DLL arama yoluna ekler.
llama-cpp-python>=0.3.0

# --- Performans ölçümü (uygulama içi metrik bloğu ve benchmark.py) ---
psutil>=5.9.0                  # CPU / RAM ölçümü
nvidia-ml-py>=12.0.0           # GPU / VRAM ölçümü (NVIDIA; yoksa otomatik atlanır)

# --- Paketleme (.exe) ---
pyinstaller>=6.6.0
